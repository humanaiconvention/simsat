# --- Project Specific ---
./dataset/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# --- Virtual Environments ---
.venv/
venv/
ENV/

# --- IDEs ---
.vscode/
.idea/

# --- OS Specific ---
.DS_Store
Thumbs.db
# --- Build artifacts and deps (added 2026-04-22 reconciliation) ---
node_modules/
**/node_modules/
.pytest_cache/
.pytest_tmp/
.pytest_tmp*/
.pytest_run/
.ptmp/
.test_tmp/
.quickstart_observation_vla_eval.md
.mypy_cache/
*.pyc
weights/

# Large data files — too big for GitHub (>100 MB)
src/sim/data/haic/stimuli.json

# --- Secrets / env (added 2026-04-25 collaborator-readiness pass) ---
.env
.env.local
.env.*.local
.envrc

# --- Local databases / runtime state ---
*.sqlite
*.sqlite3
*.db
data/lattices/*.json
data/security_log/

# --- Generated training exports (large, regenerable from lattice pool) ---
exports/

# --- Kaggle dataset image assets (regenerable from exports/ via prepare_dataset.py) ---
datasets/*/images/

# --- Generated review queue artifacts (regenerable; change with every label) ---
REVIEW_BACKLOG.md
review_queue.txt
review_queue_defer.txt

# --- Local session guides (not for repo) ---
MORNING_PUSH_GUIDE.md

# --- Local Kaggle dataset staging (zip + metadata copies for `kaggle datasets create/version`) ---
diloco_kaggle_staging/

# --- Notebook state ---
**/.ipynb_checkpoints/

# Submission markdown reports are intentionally tracked — collaborators read
# the committed snapshot. They will show as modified after regeneration; that's
# expected. Use `git checkout -- <file>` to discard regen noise before commits.

# VirtioFS artefacts and SQLite WAL files
.fuse_hidden*
*.sqlite3-wal
*.sqlite3-shm

# scratch files
review.txt

# --- Local Kaggle pull / kernel-output cache (regenerable from kaggle CLI) ---
.kaggle_output*/
.kaggle_pull*/

# --- Kaggle notebook downloads at root (clutter from kaggle kernels output) ---
/simsat-lfm2-5-vl-*.ipynb
/simsat-gemma4-*.ipynb

# --- Operator-review label downloads from gallery_review.html ---
/labels*.json
/labels*(*).json

# --- Generated review tool output ---
/gallery_review.html

# --- Test artifacts (one-off API probes etc.) ---
/test_sentinel*.png

# --- Materializer snapshots (regenerable; debug data only) ---
scripts/_*_snapshot.json

# --- Demo video build artifacts (regenerable from build_video.py) ---
video_assets/silent_demo.mp4
video_assets/simsat_demo_final.mp4
video_assets/frames/*.png
video_assets/segments/*.mp4
